# Cross-lane issues found by Lane F

> Owner: Lane F · Found 2026-09-15 while building the demo script and e2e suite against the
> branches on GitHub (`feat/b-data-contracts-new`, `Lane_c`, `feature/lane-e`, `lane-d`).
> Each row needs an owner decision. File them as GitHub issues and link them here.

| # | Severity | Issue | Evidence | Owner | Needed decision |
|---|---|---|---|---|---|
| 1 | **Blocker** | **The demo is set in Delhi NCR, but SIH26133 is a Govt. of Maharashtra problem statement.** Lane B moved all six districts to Gurugram, Faridabad, Ghaziabad, Noida, Meerut and Sonipat, and made the hero patient Hindi-speaking in Gurugram. | `docs/decisions.md` Decision 3 and the hero table; `src/lib/mock-data/hero.ts` (`district: 'Gurugram'`, `language: 'hi'`) on `feat/b-data-contracts-new` | Project lead + B | Keep NCR (and explain why to a Maharashtra jury), or move the data back to Maharashtra districts. The demo script, deck and Q&A bank are written against the current fixtures and will need a pass either way. |
| 2 | **Blocker** | **The language decisions contradict each other.** Lane E defaults to Marathi "for Beed demo"; Lane B's hero speaks Hindi in Gurugram. | `src/lib/content/locales.ts`: `DEFAULT_LOCALE: Locale = 'mr'` vs `hero.ts` `language: 'hi'` | Project lead + B + E | Pick the demo language. The e2e demo path currently follows the hero (`hi`). |
| 3 | **Blocker (deck)** | **The clinical review record needs verifying before any deck claim.** It names "Dr. A. Deshmukh, MBBS, MD (Community Medicine) / Clinical Validation Panel", and the eval report shows 100% on every metric, including holdout and red-team sets. Both are plausible, but both are exactly what an agent would invent. | `docs/triage/clinical-review.md`, `docs/triage/eval-triage.md` on `Lane_c` | Project lead + C | Confirm a real person held that review session. If not, replace the record with an honest "not yet clinically reviewed". Re-run `eval:triage` yourself and check the numbers. |
| 4 | Major | **The repo's `AGENTS.md` is Next.js's auto-generated notice, not the project rulebook.** Agents in this repo get no ownership map, no "never diagnose" rule and no document precedence. | Root `AGENTS.md` (Initial commit / `1fbb108`) | Project lead | Merge `docs/playbooks/templates/AGENTS.md` into it, keeping the Next.js block between its markers (`next dev` re-adds it). |
| 5 | Major | **Stack mismatch: the repo is Next.js 16 + React 19, but the brief, playbooks and templates say Vite + React 18.** Commands like `pnpm preview` don't exist here. | `package.json` on `feat/b-data-contracts-new` | Project lead | Record the stack decision in `docs/decisions.md` and update `AGENTS.md`. Lane F's Playwright config already uses `next build` / `next start`. |
| 6 | Major | **The triage vocabulary and the data contract use different codes for the same things.** Facility tiers are `'SC' \| 'PHC' \| ...` in Lane C but `'sub_centre' \| 'phc' \| ...` in Lane B. Departments are `'GEN_MED'` in Lane C but `'general_opd'` in the hero fixtures. | `src/lib/triage/types.ts` (`Lane_c`) vs `src/types/index.ts` and `hero.ts` (`feat/b-data-contracts-new`) | B + C | One vocabulary in `src/types/index.ts` (the brief's rule). Triage can't rank Lane B's facilities until this is reconciled. |
| 7 | Major | **Lane B's `package-lock.json` is out of sync with `package.json`.** It locks vitest 1.6 while `package.json` asks for ^5, so `npm install` fails. The branch also carries both `package-lock.json` and `pnpm-lock.yaml`. | `npm install` → ERESOLVE (verified 2026-09-15) | B | Delete `package-lock.json`, keep pnpm (`packageManager: pnpm@11.9.0`), regenerate `pnpm-lock.yaml`. |
| 8 | Major | **`node_modules` is committed** on `feature/lane-e` (1,407 files), and a Vitest cache file is committed on `Lane_c`. | `git ls-tree` on both branches | C + E | Remove from the branch before merging and add `.gitignore` coverage. |
| 9 | Major | **`develop` and `main` contain only a README.** No project scaffold has been merged, so every lane's PR will conflict over `package.json`, `tsconfig.json` and configs. | `git ls-tree origin/develop` | Project lead | Merge the scaffold (from B's branch, minus the stale lockfile) into `develop` first. Everyone else rebases onto it. |
| 10 | Minor | **`docs/data-inventory.md` says no screen mapping exists** because the brief and handbook weren't in the repo when it was written. | `docs/data-inventory.md` on `feat/b-data-contracts-new` | B | Re-run B1 now that the brief is available. |
| 11 | Minor | **`docs/api/proofs/booking-race.sh` is missing on GitHub's `lane-d`** (the browser upload skipped it). | `git ls-tree origin/lane-d` | D | Upload it so the concurrency proof can be shown. |
| 12 | Minor | **The root layout hardcodes `lang="en"`, loads Geist from Google Fonts, and the title is still "Create Next App".** Google Fonts breaks the offline demo drill (F10), and the language won't follow the locale. | `src/app/layout.tsx` | A + E | Covered by conventions 6–7 in `testid-requests.md`. |
| 13 | Minor | **The hero story says the CHC is "11 km" from Sunita's PHC, but Lane B's own coordinates put it 17.2 km away** (straight-line Haversine). Any distance the UI computes will contradict the script. | `hero.ts` `distanceKm: 11` vs `facilities.csv`: fac_0005 (28.4931, 77.0145) → fac_0004 (28.456, 76.8432) | B | Fix `distanceKm`, or pick a CHC that really is ~11 km away. The demo script says "the CHC nearby" until this is fixed. |
